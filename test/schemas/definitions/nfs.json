{
    "properties": {
        "type": {
            "enum": [
                "nfs"
            ]
        },
        "remotePath": {
            "type": "string",
            "pattern": "^(/[^/]+)+$"
        },
        "server": {
            "$ref": "./server.json"
        }
    },
    "required": [
        "type",
        "server",
        "remotePath"
    ],
    "additionalProperties": false
}